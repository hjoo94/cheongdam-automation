```
================================================================
        청담 자동화 프로그램 작업 인수인계서
        전달: Claude → Cursor Pro
        문서 갱신: Cursor (저장소 실제 상태 반영)
        날짜: 2026-04-14
        앱 버전: v1.0.30
================================================================
```

## 1. 프로젝트 기본 정보

| 항목 | 내용 |
|------|------|
| 저장소 | https://github.com/hjoo94/cheongdam-automation |
| 브랜치 | `main` |
| 로컬 예시 경로 | `C:\Users\DESKTOP\Desktop\코덱스` |
| 운영 서버 (문서/팀 기준) | AWS Lightsail **43.201.84.136:4300** |
| 코드 저장소 기본 URL | `server.js` / 클라이언트 `main.js`의 폴백은 **`http://43.203.124.132:4300`** — 실제 운영 IP와 다르면 **서버 `.env`의 `PUBLIC_BASE_URL`** 과 **클라이언트 `settings.json`의 `serverBaseUrl`** 을 운영 IP(또는 도메인)에 맞출 것 |
| 클라이언트 패키지 버전 | `02_client_app/baemin-review-bot/package.json` → **1.0.30** |
| 기술 스택 | Electron 28, Playwright 1.44, Node.js |

### 1.1 Git `main` 최신 상태 (2026-04-14 로컬 확인 기준)

다음 순서로 **이미 `origin/main`에 반영된 커밋**이 있음 (Claude 메모의「최종 커밋 = 90e24d2」는 **기능 패치 커밋**이며, 그 이후 문서·파일명 폴백 수정이 추가됨).

| 커밋 | 설명 |
|------|------|
| `1ebbd4c` | docs: 인수인계서 오탈자 수정 (해피톡) — **문서 최신** |
| `9bb216d` | docs: v1.0.30 기준 인수인계서 (서버·라이선스·자동업데이트) |
| `4105581` | fix: CLIENT_APP_FILE fallback 1.0.29 → 1.0.30 |
| `90e24d2` | fix: v1.0.30 coupang happytalk steps 확장 + naver mail 매칭 로직 개선 |

**다음 작업 전 필수:** `git fetch origin main` 후 `git log --oneline -5`로 원격과 로컬이 위와 같은지 확인.

---

## 2. 파일 구조 핵심 경로

| 역할 | 경로 |
|------|------|
| 라이선스·업데이트·GPT 프록시 서버 | `03_license_server/server.js` |
| 클라이언트 앱 (Electron) | `02_client_app/baemin-review-bot/` |
| 쿠팡 해피톡·블라인드 등 | `platforms/coupangAnswered.js` |
| 네이버 메일 ↔ 리뷰 로그 | `platforms/naverMail.js` |
| 라이선스·자동업데이트 진입 | `app/main.js` |
| 클라이언트 빌드 | 디렉터리 `02_client_app/baemin-review-bot` 에서 `npm run build` (또는 `npm run build:win`) |

**참고:** 클라이언트에 `src/` 폴더는 없음. 소스는 `app/`, `platforms/`, `bot/`, `utils/` 등 루트 하위에 있음.

---

## 3. GitHub vs 로컬 동기화 확인 (최우선)

Claude 지시대로 주기적으로 확인.

**PowerShell 예시**

```powershell
cd C:\Users\DESKTOP\Desktop\코덱스
git fetch origin
git status
git log --oneline -5
Select-String -Path "03_license_server\server.js" -Pattern "CLIENT_APP_VERSION|CLIENT_APP_FILE|PUBLIC_BASE_URL"
```

**저장소(폴백) 기대값 — `03_license_server/server.js`**

- `CLIENT_APP_VERSION` 폴백 → **`'1.0.30'`**
- `CLIENT_APP_FILE` 폴백 → **`'Cheongdam Bot Setup 1.0.30.exe'`**
- `PUBLIC_BASE_URL` 폴백 → **`http://43.203.124.132:4300`** (운영이 43.201.84.136이면 **반드시 `.env`로 덮어쓰기**)

로컬이 뒤처졌다면: `git pull origin main`  
로컬만 앞서 있다면: `git push origin main`  
**`git push --force`는 저장소 히스토리를 망가뜨릴 수 있으니, 팀 합의·비상 시에만 검토.**

---

## 4. v1.0.30 패치 내역 (코드 기준 사실 반영)

### 4.1 `platforms/coupangAnswered.js`

- `HAPPYTALK_ACTION_DELAY_MS` 기본값 **350ms**
- `waitForHappyTalkReady`: 키워드 배열 확장, `openCoupangHappyTalkPage`에서 **sleep(900)** 및 **`waitForHappyTalkReady(page, 20000)`**
- `submitCoupangBlind`: **6단계** `steps` (choices / nextChoices / timeout / `verifyChange: false` 등) 확장

### 4.2 `platforms/naverMail.js`

- `findBestLogMatch`: `FUZZY_MIN` **본문 길이에 따른 동적 값**(약 0.2 ~ 0.48)
- 닉네임 없는 행 스킵 조건 완화: `sameReviewId`·유사도 등과 연동
- `latestNicknameFallback`: **`targetReviewDate`가 있을 때 동월(`extractYearMonth`) 엔트리만** 사용
- `processOneMail`: 닉네임 없을 때 **콘솔 경고 + `writeMailTrace('nickname.missing')` + `appendUserError` 후 `throw`** — 로그 메시지에「skip」이 있으나 **실제로는 예외로 해당 메일 처리 흐름이 중단**됨(상위 러너가 catch 시 건너뛰기 여부는 호출부에 따름)

### 4.3 `03_license_server/server.js`

- `UPDATE_MANIFEST.client.version` 폴백 **1.0.30**
- `UPDATE_MANIFEST.client.fileName` 폴백 **Cheongdam Bot Setup 1.0.30.exe**

### 4.4 `02_client_app/baemin-review-bot/package.json`

- `"version": "1.0.30"`

---

## 5. 환경 변수 우선순위 (중요)

Node는 **이미 프로세스에 설정된 `process.env`가 있으면** `server.js`가 읽는 `.env` 로더가 덮어쓰지 않는 구현일 수 있음. 운영에서는 다음을 구분할 것.

1. **서버 셸 / systemd / pm2** 에 넘긴 환경 변수  
2. **`03_license_server/.env`**  
3. **`server.js` 내 폴백 문자열**

따라서 **`server.js` 폴백만 수정했다고 해서**, 서버에 **`CLIENT_APP_VERSION` / `CLIENT_APP_FILE` / `PUBLIC_BASE_URL` 이 `.env`에 박혀 있으면** 매니페스트는 계속 예전 값일 수 있음. 배포 후 **`Select-String` 또는 서버에서 `GET /api/updates/client/latest` 응답 JSON**으로 최종 확인.

---

## 6. 라이선스·자동 업데이트 (한 줄 요약)

- 라이선스: 클라이언트 → **`POST /api/license/verify`** → 서버 `data/licenses.json` 기준 검증.  
- 자동 업데이트: 클라이언트 → **`GET /api/updates/client/latest`** → `version` / `url` / `sha256` / `installerReady` → `downloads/` 아래 **실제 exe 파일명·해시**와 일치해야 사용자 PC에 패치 적용.

**배포 체크:** `03_license_server/downloads/`에 **`CLIENT_APP_FILE`과 동일한 파일명**의 설치 파일 업로드 → API에서 `installerReady: true` 및 `sha256` 64자 확인.

---

## 7. AI 워크플로우 역할 분담 (Claude 지시 유지)

| 역할 | 담당 |
|------|------|
| Claude (전략) | 아키텍처·로직 흐름·프롬프트·작업 지시서 |
| Cursor Pro (실행) | 로컬 수정, 터미널, Git, 저장소 일관성 검증 |
| ChatGPT Codex (예비) | 향후 AWS 자동화 등 (팀에서 활성화 시) |

---

## 8. 지금 당장 할 일 (우선순위)

1. **동기화:** `git fetch` / `git log --oneline -5` / 필요 시 `git pull` 또는 `git push`  
2. **AWS:** Lightsail SSH → 저장소 경로에서 `git pull origin main` → **`server.js` 또는 `.env` 버전·파일명** 확인 → **pm2 restart** 등으로 프로세스 재시작  
3. **빌드:** `02_client_app/baemin-review-bot` 에서 `npm run build` (또는 `build:win`) → 산출물명이 **`Cheongdam Bot Setup 1.0.30.exe`** 와 정책상 맞는지 확인  
4. **운영 확인:** 네이버 메일 매칭·쿠팡 해피톡 6단계(로그·스크린샷·`user-errors` JSONL)

---

## 9. 다음 로드맵 (참고)

- 단기: Desktop(Electron) **v1.0.30** 안정화·현장 피드백 반영  
- 중기: **모바일 웹** (`05_mobile_mockup/` 등) — 쿠팡/배민/캐시노트/AI 모드 탭 구조는 설계 단계에서 Claude와 합의  
- 이후: Codex 활용 **AWS 배포 자동화** (팀 결정 시)

---

## 10. 긴급 시 참고

- 기능·로직 지시서: Claude에게 현재 이 문서 + `git log` + 재현 로그를 함께 전달  
- 서버 응답 이상: **Lightsail 인스턴스 재시작**, 방화벽·4300 포트, **`PUBLIC_BASE_URL`과 실제 접속 URL 일치**  
- GitHub 푸시 거절: **대용량 바이너리**가 `git add .`에 포함되지 않았는지 확인 (과거 900MB급 ZIP으로 푸시 실패 사례 있음)

---

```
================================================================
        인수인계서 끝
        다음 작업 전: git fetch && git log --oneline -5
        서버 반영 후: GET /api/updates/client/latest JSON 확인
================================================================
```
